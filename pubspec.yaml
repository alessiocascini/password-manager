name: password_manager
description: Secure Dart password manager with master auth & encrypted storage.
version: 1.1.0
repository: https://github.com/alessiocascini/password-manager.git

environment:
  sdk: ^3.7.1

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0
  encrypt: ^5.0.3
  crypto: ^3.0.6
  pointycastle: ^3.9.1

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
